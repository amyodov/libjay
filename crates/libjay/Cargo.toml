[package]
name = "libjay"
description = "Embedded J and APL: compile array-language expressions from string literals, run them in parallel"
readme = "../../README.md"
keywords = ["j", "apl", "array", "vectorization", "language"]
categories = ["compilers", "mathematics", "science"]
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "jay"

[dependencies]
rayon = "1.12.0"

[dev-dependencies]
rstest = "0.25"
