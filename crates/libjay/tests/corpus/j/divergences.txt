// Where libjay answers differently from jconsole on purpose. The `? ` line
// says why. The snapshot records BOTH answers, so `jay-corpus record`
// re-measures the pair and fails if it has quietly converged — the signal
// that the note (and the entry in docs/coverage.md) should go.

// J has three character types — one byte per item, two, and four — and
// libjay has one. A literal is the byte-oriented type, which is what every
// corpus/j/literals.txt line holds libjay to; `u:` widens one, and the
// wider type differs from the narrow one only in how it is WRITTEN OUT,
// since the items and their codes are the same either way (`# u: 'é'` is 2
// and `3 u: u: 'é'` is `195 169` in both). libjay writes a character below
// 256 as that byte, so a widened literal shows the text it was written as
// rather than the two characters its bytes name, and a character `u:` made
// from a code between 128 and 255 shows as a byte no text can hold.
// Everything a wide character does apart from displaying agrees, and a code
// at 256 or above — every one that needs the wide type to exist — displays
// alike too.
u: 'é'
? a widened literal writes its bytes here and its characters there; reason: model
~ cause=differ:val:atom/text|differ:val:vector/text|differ:val:table/text|differ:val:planes/text|differ:val:empty/text|differ:val:atom/num|differ:val:vector/num|differ:val:table/num verb=u: also=^:,&.,&.:,M.,f.,::,:,!:,F..,F.:,F::,p.,p:,q:,;.,*:,+:,-:,+,-,*,%,^,^.,<.,>.,=,~:,<:,>:,|,e.,i.,x:,b.,#.,#:,j.,r.,o.,!.,H.,=.,=:
u: 233
? a character between 128 and 255 writes as its byte here; reason: model
10 u: 233
? a character between 128 and 255 writes as its byte here; reason: model
3!:0 u: 'é'
? libjay has one character type, so widening does not change it; reason: model
3!:0 (u: (0 $ 0))
? the widened type is what `u:` ALWAYS reports there, empty or not; reason: model
3!:0 (u: 'abc')
? the same over a literal that needed no widening at all; reason: model
3!:0 (u: 97)
? the same over a code point below 128; reason: model
3!:0 (10 u: (0 $ 0))
? `10 u:` reports the FOUR-byte type there, which libjay has no more than the two; reason: model

// The same one-character-type reading over the byte conversions: `2 u:`
// widens bytes into characters in J, which is where the two writings part,
// and a codepoint between 128 and 255 that `9 u:` answers with writes as
// its byte here and as its character there.
2 u: 8 u: u: 955
? widening bytes to characters changes only how they are written out; reason: model
9 u: 200 200
? a character between 128 and 255 writes as its byte here; reason: model

// Two whole numbers a double cannot tell apart. jconsole holds every
// number in a double once the verb is not one of the integer special
// cases, so `9007199254740992` and `9007199254740993` are the same value
// to its `*.`; libjay keeps them as the integers they were written as and
// answers their real least common multiple. The comparison tolerance is
// not what parts the two — the numbers are equal in the reference's
// arithmetic before any tolerance is consulted.
(9007199254740992) *. (9007199254740993)
? jconsole holds both in one double and libjay keeps them exact; reason: proven-wrong

// Two large arguments to `!` a double cannot tell apart. jconsole answers
// `1e10 ! (1e10 + 1e_4)` with 1.00234; the value is 1.0023631, which the
// digamma series gives independently, and the reference's difference of two
// log-gammas cancels catastrophically at that size. libjay's own quotient
// lands on 1.00238, no closer, and matching the reference here would mean
// matching its arithmetic rather than the mathematics. Where the difference
// of the two arguments is a small WHOLE number both sides are exact and
// agree, which is the ordinary case and is recorded in fuzz_found.txt.
//
// The rows below are the same divergence at a size the REFERENCE can
// answer quickly: `1e10 ! (1e10 + 1e_4)` costs jconsole twenty seconds on
// its own, which is most of a sweep's patience, and a row the interpreter
// does not finish measuring is a row that silently excuses nothing. The 1
// is subtracted off because the difference is in digits the printer would
// otherwise round away.
_1 + ((100000000) ! (100000000.00000001))
? both sides lose this one; the reference's answer is not the value either; reason: proven-wrong
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/text|differ:val:vector/text verb=! also=%,*,+,-,+:,-:,*:,<:,>:,j.,&.,&.: answers=inexact,near
_1 + ((1e8) ! (1e8 + 1e_4))
? both sides lose this one; the reference's answer is not the value either; reason: proven-wrong

// FACTORISING SOMETHING THAT IS NOT A WHOLE NUMBER: THE REFERENCE ANSWERS
// NOTHING AT ALL. `2 p: (3j4 1j_1)` prints no value there — and neither
// does `$` of it, nor `3!:0` of it, so there is no shape and no type to
// read either. That is the signature of the SILENT DEATH the quarantine
// list already carries for `q: _.`, which is the same verb: `3 p: y` IS
// `q: y`. An answer with no readable type cannot be followed from outside
// the interpreter, so libjay refuses a factorisation of anything that is
// not a whole number and says which kind of data it was handed.
//
// THE NaN AND `q:` SPELLINGS OF THIS ARE NOT RECORDED HERE AND MUST NEVER
// BE: `q: _.` and `n q: _.` kill jconsole silently, so a corpus file
// holding one would take the recording down with it. The rule below
// matches them by cause instead.
2 p: (3j4 1j_1)
? the reference answers a factorisation of a complex number with nothing at all; reason: abort
~ cause=we-refuse:err:domain_error:_a_prime_query_needs_an_integer|we-refuse:err:domain_error:_prime_factors_need_an_integer|we-refuse:err:domain_error:_prime_exponents_need_an_integer verb=p:,q: also=j.,r.,#:,#.,o.,F..,F.:,F::,M.,^,^.,%:,+,-,*,%,^:,L:,S:,-.,>,e.,~:,=,<:,>:,<.,>.,|.,{.,}.,|,.,!.,x:,u:,b.,f.,::,p.,p..,+.,*.,i:,":,A.,I.,/.,E.,s:,%.,+:,-:,*:,a.

// A SINGULAR MATRIX UNDER `%.`, WHERE THE REFERENCE HAS NO RULE — ONLY THE
// LUCK OF ITS OWN FACTORISATION. Four rank-deficient arguments, measured:
//
//   `%. (2 2 $ 1 1 1 1)`            a NaN ERROR
//   `%. (3 2 $ 1 2 2 4 3 6)`        a NaN ERROR
//   `%. (3 3 $ 1 2 3 4 5 6 7 8 9)`  a table of NaN VALUES
//   `%. (i. 4 3)`                   a table of NaN VALUES
//
// Every one of them is rank-deficient and every one of them is the same
// question; two are refused and two are answered. What separates them is
// whether the factorisation lands on an EXACT zero pivot or merely a tiny
// one — the reference's arithmetic rather than the mathematics, which is
// what round 5B concluded about the float GCD as well. libjay refuses a
// singular system consistently and says so, which is the answer the
// reference gives to half of these.
%. (i. 4 3)
? the reference answers a rank-deficient system with NaNs and refuses others; reason: self-contradiction
$ %. (i. 4 3)
? the reference answers a rank-deficient system with NaNs and refuses others; reason: self-contradiction
%. (3 3 $ 1 2 3 4 5 6 7 8 9)
? the reference answers a rank-deficient system with NaNs and refuses others; reason: self-contradiction

// THE INVERSE OF A NaN, IN A TABLE OF ONE ITEM. The reference widens a
// VECTOR holding a NaN to the complex type — `%. (, _.)` and `%. (_. 1 2)`
// are `_.j_.` there, which round 9 followed — and it widens the ONE-ITEM
// TABLE with it, while a wider table stays real: `%. (1 1 $ _.)` is
// `_.j_.` and `%. (2 2 $ _. 1 2 3)` is the real `_.`. That last split is
// the item count deciding a type again, with no rule behind it, so the
// table is left as it stands.
%. (1 1 $ _.)
? the reference widens a one-item NaN table to complex where a wider one stays real; reason: self-contradiction

// AN INFINITY AMONG THE PRIMES, WHERE THE REFERENCE CONTRADICTS ITSELF
// BETWEEN ONE ITEM AND SEVERAL. `4 p: (_ 1 2)` is `2 2 2` there, although
// the reference's own `4 p: 2` is 3: an infinity ANYWHERE makes every cell
// answer with the smallest prime, the cells holding ordinary numbers
// included. libjay keeps ONE rule applied cell by cell, so no cell changes
// what a neighbouring cell answers.
//
// The NaN half of this note was followed in round 9D rather than pinned:
// `4 p: _.` is a domain error there and `4 p: (_. 1 4)` is `2 2 5`, and
// libjay now answers both as the reference does — a NaN has no next prime
// alone and reads as one below every prime in company. The two forms that
// ask whether a value IS prime were followed in round 9B.
4 p: (_ 1 2)
? an infinity makes the reference answer 2 for cells whose own answer is 3; reason: self-contradiction

// THE NEAR-EQUAL BINOMIAL REACHING A REFUSAL rather than a value, which is
// to the rule above what the GCD's refusal row is to the GCD's. `1 ! y` IS
// y, so `(1) ! (0.9999999999999999)` is 0.99999999999999989 here and the
// reference reads it tolerantly as exactly 1. A verb downstream that wants
// a WHOLE number then refuses on one side and not the other: a format
// specification is whole numbers, so libjay refuses where the reference
// formats. The answers cannot be classified as numbers — one of them is a
// refusal and the other is text — so this needs a rule of its own rather
// than the `answers=` clause above.
((1) ! (0.9999999999999999)) ": 5
? the near-equal binomial parts by one bit and the format downstream refuses; reason: proven-wrong
~ cause=we-refuse:err:domain_error:_a_format_specification_is_whole_numbers|we-refuse:err:domain_error:_format_by_specification_takes_numbers verb=! also=%,*,+,-,+:,-:,*:,<:,>:,j.,":,&.,&.:

// `x C. y` over a left argument that is no permutation of y's items. The
// Dictionary defines the dyad for a permutation — a direct one or a boxed
// list of cycles — and says nothing about anything else. Thirty probes did
// not find a rule in what the reference does with one: `(_2 0 2) C. 'hello'`
// is `eolhl` there, `(_1 _2) C. i.4` is `0 1 3 2` and `(_2 _1) C. i.4` is
// `0 1 2 3`, which is neither the direct reading nor the flat cycle form
// `C. y` writes, and `(_1) C. i.4` and `(_3) C. (1;2;3)` disagree with both
// again. libjay refuses instead: an undefined argument is answered with a
// diagnostic that says which value makes it no permutation, rather than
// with a number fabricated to match behaviour nobody has specified.
(_2 0 2) C. 'hello'
? the reference's answer for a non-permutation is undefined; libjay refuses; reason: undefined
(_3) C. (1;2;3)
? the reference's answer for a non-permutation is undefined; libjay refuses; reason: undefined
(_1 _2) C. i.4
? the reference's answer for a non-permutation is undefined; libjay refuses; reason: undefined
(_2 _1) C. i.4
? the reference's answer for a non-permutation is undefined; libjay refuses; reason: undefined
(_1) C. i.4
? the reference's answer for a non-permutation is undefined; libjay refuses; reason: undefined
C. (_2 0 2)
? the reference's answer for a non-permutation is undefined; libjay refuses; reason: undefined

// `x ^. y` over a boxed y and a NEGATIVE x, which sends the logarithm into
// the complex domain. jconsole answers this one with NOTHING AT ALL — no
// value, no error, no output line — while `2 ^. (<1)`, the same sentence
// with a positive left argument, is a domain error. The Dictionary defines
// the logarithm on numbers, so a box is undefined either way; libjay
// refuses both alike rather than following the silence.
(_3) ^. (<1)
? jconsole answers this one with no output at all and refuses 2 ^. (<1); reason: abort
(_3) ^. (<'abc')
? jconsole answers this one with no output at all and refuses 2 ^. (<1); reason: abort
(_3) ^. (1;2)
? jconsole answers this one with no output at all and refuses 2 ^. (<1); reason: abort

// `x I. y` over BOXES. The Dictionary defines the interval index for a
// sorted numeric list; libjay extends it to every element type that has an
// order, which is the same total order `/:` grades by — and for characters
// and symbols the reference agrees. For BOXES it does not: its comparison
// there tells apart only equal from unequal, so `(<2) I. (1;2;3)` is
// `1 0 1` — a 1 on both sides of the 0 — which no order can produce, and
// `(<5) I. (1;2;3)` is `1 1 1` although `/: ((<5);1;2;3)` puts the 5 last
// in both. libjay answers by the order it grades with.
(<'') I. (1;2;3)
? the reference's box comparison here tells apart only equal from unequal; reason: self-contradiction
(<'a') I. (1;2;3)
? the reference's box comparison here tells apart only equal from unequal; reason: self-contradiction
(<5) I. (1;2;3)
? the reference's box comparison here tells apart only equal from unequal; reason: self-contradiction
(<2) I. (1;2;3)
? the reference's box comparison here tells apart only equal from unequal; reason: self-contradiction

// A polynomial of the THIRD degree or higher with an infinite coefficient
// aborts jconsole's own root finder — `p. (_ 1 2 3)` and `p. (1 2 3 __)`
// are both stack errors there, which is an interpreter limit and not an
// answer the Dictionary describes. The same polynomials at the SECOND
// degree answer with roots that have no value, and libjay carries that
// reading up to any degree: an infinite leading coefficient over a
// non-zero constant term leaves no root, and the roots come back as
// `_.j_.`.
p. (_ 1 2 3)
? jconsole's root finder aborts on this one with a stack error; reason: abort
p. (1 2 3 __)
? jconsole's root finder aborts on this one with a stack error; reason: abort
p. (0 1 1 _)
? jconsole's root finder aborts on this one with a stack error; reason: abort
// An LCM of a whole number a double cannot hold. The reference contradicts
// ITSELF here rather than merely rounding: `9223372036854775806 +. 5` is 1
// there, so `x *. y` — `(x % (x +. y)) * y` by the Dictionary's own
// definition — is five times x, and the reference answers x instead. It
// holds 9223372036854775806 as 9223372036854775808, whose residue by 5 its
// own float arithmetic loses. libjay keeps the value exact and answers the
// LCM the reference's own GCD asks for.
(9223372036854775806) *. (,5)
? the reference's own GCD of 1 contradicts the LCM it answers; reason: self-contradiction
~ cause=differ:val:atom/num verb=*.,+. also=%:,&.,&.: answers=inexact,huge
(_9223372036854775806) *. "1 _ 0 (i. 5)
? the same boundary, one radix per cell; reason: self-contradiction
(_9223372036854775806) *."_2 (i. 3 4)
? the same boundary over a table; reason: self-contradiction
(2 7 1 8) *. (_9223372036854775806)
? the same boundary, the huge number on the right; reason: self-contradiction
~ cause=differ:val:vector/num|differ:val:table/num|differ:val:planes/num verb=*.,+. also=%:,&.,&.: answers=inexact,huge

// A LOGARITHM whose left argument holds a NEGATIVE number and whose right
// is not numeric. The reference neither answers it nor refuses it: the
// sentence leaves no value, prints nothing on either stream, exits 0, and
// the session carries on — `z =: (1.5 2.5 _0.5) ^. a:` leaves `z`
// undefined. The same box in the same place IS a domain error where no
// atom of the left is negative (`(1.5 2.5) ^. a:`), so an unrelated atom's
// SIGN decides whether a box is in the domain. libjay refuses on the type,
// which is what it answers everywhere else on this path.
(1.5 2.5 _0.5) ^. (a:)
? the reference neither answers nor refuses; the sign of an unrelated atom decides; reason: abort
(1.5 _0.5) ^. (<1 2)
? the same silence over a plain box; reason: abort
(_0.5) ^. 'a'
? the same silence over characters; reason: abort
(1.5 2.5 _0.5) ^. ('hello')
? the same silence over a longer character list; reason: abort
(_ __ 0) ^. (1;2 3)
? the same silence with the infinities for the negative atom; reason: abort

// INTERVAL INDEX against a SINGLE EXACT bound. The reference answers
// `(1x) I. 2` with 0 and `(1x 2x) I. 2` with 1: adding a bound that is NOT
// below the value cannot raise the count from none to one, and the same
// bound written as a float — `(,1) I. 2` — is 1 there. libjay counts the
// bounds below the value whatever type holds them.
(1x) I. 2
? the reference's own two-bound answer contradicts this one; reason: self-contradiction
(_5x) I. 2
? the same, with a negative bound; reason: self-contradiction
(1r2) I. 2
? the same, with a rational bound; reason: self-contradiction

// A LOGARITHM TO ITS OWN NEGATIVE BASE. `x ^. x` is 1 for every x, and
// libjay answers 1. jconsole answers a complex number a rounding away from
// it, and a DIFFERENT one for every spelling of the same value: `_3 ^. _3`
// is `1j_2.09983e_18`, `_3x ^. _3x` is `1j9.81297e_18`, `_3 ^. _3x` is
// `1j5.34113e_17` and `_3x ^. _3` is `1j_4.56982e_17`, the last three with
// a real part of 0.99999999999999989 rather than 1. Four answers to one
// question is the reference contradicting itself, so there is nothing to
// follow; libjay gives the value the algebra gives.
_3 ^. _3
? the reference answers a rounding away from 1, differently per spelling; reason: self-contradiction
_3x ^. _3x
? the reference answers a rounding away from 1, differently per spelling; reason: self-contradiction
_3 ^. _3x
? the reference answers a rounding away from 1, differently per spelling; reason: self-contradiction
_2 ^. _2
? the reference answers a rounding away from 1, differently per spelling; reason: self-contradiction
_1r2 ^. _1r2
? the reference answers a rounding away from 1, differently per spelling; reason: self-contradiction

// A GCD OR LCM OF TWO VALUES WITH NO COMMON MEASURE. Neither engine can
// answer `(o. 1) +. 1` — pi and 1 have no common divisor — and both run a
// float Euclid until the residues reach the noise. The residue chains
// AGREE step for step (jconsole's own `|` was measured against libjay's
// over thirty pairs and matched exactly); the two part only in where the
// chain is cut, and the reference's cut leaves an answer that is not a
// common divisor of anything: `g | 1` is 2.27e_13 and `g | (o. 1)` is
// 2.52e_13, neither of them zero, and its `(o. 1) +. 3` is not a multiple
// of its `(o. 1) +. 1` although a common divisor of pi and 1 must divide
// a common divisor of pi and 3. libjay cuts the chain where the residue
// falls below the comparison tolerance times the larger argument, which is
// a rule that can be stated; the reference's could not be recovered from
// forty-four measured pairs.
(o. 1) *. 1
? the two float Euclids cut the same residue chain at different steps; reason: proven-wrong
1e12 * ((o. 1) +. 1)
? the two float Euclids cut the same residue chain at different steps; reason: proven-wrong

// THE WHOLE `+.`/`*.` FLOAT BOUNDARY, AS ONE FAMILY. A sweep draws a fresh
// spelling of it every few thousand sentences and never the same one twice,
// so neither the sentence nor the cause signature — which is
// `differ:val:atom/num` for every arithmetic difference there is — can
// match it. The family rule below is what does. What it covers is exactly
// what the two notes above say: a GCD or an LCM whose two arguments the
// engines' float arithmetic does not agree about, either because they have
// no common measure (the residue chains are cut at different steps) or
// because they are two whole numbers one double cannot tell apart. It asks
// for a numeric answer with a float written in it, and lets the sentence
// name, besides the GCD itself, only arithmetic that can BUILD such a pair
// — pi, a root, a logarithm, a power, the four operations, a floor or a
// ceiling, a residue, the two readings of a complex number, a tolerance —
// and structure, which computes nothing of its own. A sentence naming
// anything else is not this family and stays unexplained.
//
// TWO ADDITIONS TO THAT LIST, ROUND 7, EACH WITH ITS REASON.
//
// `#.` AND `#:`. A base decode is a polynomial in the base — `+/ y * b ^ i`
// — which is a power and the four operations, all of them already on the
// list, and the encode only writes the same value back out in digits.
// Neither can part the two engines on its own, and it was measured: both
// answer `#. (1 1.0000000000001)` with the same 3.0000000000001, and it is
// the `*.` after it that parts them. Without this,
// `2 *. &.:#. (1 1.0000000000001)` — a plain LCM of a near-equal pair,
// wrapped — stays unexplained for the wrapper's sake alone.
//
// `^:`. A GCD reached through a power conjunction is still that GCD:
// `(*. o.) L:2 (|:^:_1 (i. 2 3 4))` parts on the LCM of two multiples of
// pi, and its `|:^:_1` — the obverse of a transpose — is pure
// rearrangement that both engines agree about, as measuring it alone
// shows. THE COST OF THIS ONE IS REAL AND IS WRITTEN DOWN HERE: `^:` with
// a negative left argument is an obverse, which is a family of its own,
// so a future divergence in the obverse OF `+.` or OF `*.` would be
// excused by this rule rather than reported. Those two obverses were
// measured across the exact and float types in round 7 and agree
// throughout; if this rule is ever the only thing matching a mismatch,
// re-measure them before trusting it.
(o. 1) *. 3
? the two float Euclids cut the same residue chain at different steps; reason: proven-wrong
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num|differ:val:planes/num|differ:val:atom/text|differ:val:vector/text|differ:val:table/text|differ:val:planes/text verb=+.,*. also=o.,%,%:,^.,^,*,+,-,+:,-:,*:,<.,>.,<:,>:,=,~:,|,j.,r.,!.,&.,&.:,#.,#:,^:,%.,L:,S:,f.,|:,# answers=inexact

// The same family REACHING A REFUSAL rather than a value. Where the two
// GCDs part by one bit — `1 +. 1.0000000000000002` is exactly 1 there and
// 0.9999999999999998 here — a verb downstream can refuse what it is handed
// on one side and not on the other: the logarithm of a value just under 1
// is a tiny NEGATIVE number, its imaginary reading is not tolerantly real,
// and an ordering of it carries no order. It is the divergence above,
// shown through one more verb.
(,: < j.) &.^. ((1) +. (1.0000000000000002))
? the two float Euclids part by one bit and the ordering downstream refuses; reason: proven-wrong
~ cause=we-refuse:err:domain_error:_complex_numbers_have_no_order;_only_equality_(=,_~:)_appli verb=+.,*. also=o.,%,%:,^.,^,*,+,-,<.,>.,|,j.,!.,&.,&.:

// THE OBVERSE OF THE FACTORIAL, WHERE THE EQUATION HAS MORE THAN ONE
// ANSWER. `! x = y` has two solutions between every pair of the gamma
// function's poles, so which one `!^:_1` means is a choice. libjay takes
// the smallest argument at or above zero and says so; jconsole searches
// from a seed of its own and lands elsewhere — `!^:_1 ] 1.1` is
// `_0.136587` there and 1.19699 here, both of them arguments whose
// factorial is 1.1. Its search does not always finish: `!^:_1 ] 1.5` is a
// NaN error although two arguments answer it, and `!^:_1` of the gamma
// minimum 0.885603194410888 never returns at all. There is no consistent
// rule to follow, so libjay keeps its own.
!^:_1 ] 1.1
? two arguments have this factorial and the two engines pick different ones; reason: undefined
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/text|differ:val:vector/text verb=! with=^: also=%,%:,-,+,*,&.,&.: answers=numeric
!^:_1 ] 1.0000001
? two arguments have this factorial and the two engines pick different ones; reason: undefined
!^:_1 ] 0.8
? no argument at or above zero has this factorial; the reference goes below; reason: undefined

// A REDUCTION INSIDE A CUT WHOSE ANSWER IS WHOLE. `%/ 7 1 8` is 56 and
// `<;._1 (2 7 1 8)` is the one piece `7 1 8`, but `%/;._1 (2 7 1 8)` is
// 2.76677e_322 in jconsole — 56 times the smallest denormal, which is the
// integer 56's bit pattern read as a double. `%/;._1 (2 7 1 4)` is
// 1.38338e_322 the same way, 28 bits deep, while `+/;._1` and `*/;._1` of
// the same argument are 16 and 56 as they should be. The cut copies an
// integer cell into a float frame without converting it; libjay answers
// the value.
%/;._1 (2 7 1 8)
? the reference copies the integer answer's bits into a float cell; reason: proven-wrong
%/;._1 (2 7 1 4)
? the reference copies the integer answer's bits into a float cell; reason: proven-wrong

// The obverse of the factorial BELOW ZERO. `!^:_1 (_5)` is
// `4.55654j_5.33834` there — a complex argument for a real y, and one
// whose factorial jconsole itself does not return `_5` for. libjay has no
// argument at or above zero whose factorial is negative and says so.
!^:_1 (_5)
? the reference searches into the complex plane; libjay has no real answer; reason: proven-wrong
! ^:(_1 0 1) (_5x)
? the reference searches into the complex plane; libjay has no real answer; reason: proven-wrong
! ^:_1 (3j4 1j_1)
? the reference searches the complex plane; libjay's obverse reads reals; reason: proven-wrong
~ cause=we-refuse:err:NaN_error:_this_factorial_has_no_argument|we-refuse:err:domain_error:_the_obverse_of_!_needs_numeric_data verb=! with=^: also=%,%:,-,+,*,j.,&.,&.:,r.,;.,o.,^.

// The same two whole numbers a double cannot tell apart, reached through a
// VECTOR left argument, where the whole pass goes to floats at once:
// jconsole answers every cell with 9.2233720368547758e18 although
// `4 *. (9223372036854775806)` is twice that and `5 *.` five times it, and
// its own `3 *. (9223372036854775806)` is the integer.
(3 1 4 1 5) *. (9223372036854775806)
? jconsole holds the left and the right in one double and libjay keeps them exact; reason: self-contradiction
!^:_1 (-. ^:(<2) 2)
? the reference searches into the complex plane; libjay has no real answer; reason: proven-wrong
! ^:(_1 0 1) (_9223372036854775806)
? the reference searches into the complex plane; libjay has no real answer; reason: proven-wrong
// The DYADIC OUTFIX `x u\.` over an empty frame of BOXED, CHARACTER or
// SYMBOL cells, where u is exactly `+/` and x is 2 or more. jconsole
// answers a LENGTH ERROR; libjay answers the empty frame the definition
// gives — `1 + 0 - x` outfixes is none of them, so there is nothing to
// apply u to. Every neighbour in the grid answers: `*/`, `-/`, `<./`,
// `>./`, `,/` and `<` in u's place; `0 3 $ 0` and every other numeric type
// in the argument's; `0 $ a:` where the item is itself empty; and 1, 0 or
// a negative left argument. Decisively, `4 ([: +/ ])\.` and `4 (+/@:])\.`
// — the SAME function spelled around `+/` — answer the empty where bare
// `+/` refuses, so the refusal is the reference's own special code for
// `+/\.` disagreeing with the reference's ordinary path.
4 +/\\. (0 3 $ (3 $ a:))
? the same function spelled `[: +/ ]` answers the empty there; reason: self-contradiction
2 +/\\. (0 3 $ (3 $ a:))
? the same, with the smallest left argument that refuses; reason: self-contradiction
2 +/\\. (0 3 $ 'a')
? the same over characters; reason: self-contradiction
2 +/\\. (0 3 $ s: 'a')
? the same over symbols; reason: self-contradiction
2 +/\\. (0 2 3 $ (6 $ a:))
? the same where the cells are of higher rank; reason: self-contradiction

// INTERVAL INDEX over BOXES. `x I. y` counts the bounds of a sorted x that
// lie below y, and boxes have no such order: the reference's own answers
// contradict each other. `((<1);(<2);(<3)) I. (<0)` is 3 and `I. (<9)` is 2
// — a SMALLER value cannot have MORE bounds below it — while `I. (<2)` is 3
// and `I. (<'a')` is 2. libjay counts the boxes that sort below the value
// in its own total order, which for these is none. Neither answer is the
// Dictionary's, which asks for a left argument in ascending order and says
// nothing about what ordering boxes have.
(<1 2) I. (<2)
? the reference's own answers over boxes are not monotone; reason: self-contradiction
(a:) I. (<1)
? the same, with the empty box as the bound; reason: self-contradiction
(<'a') I. (<1)
? the same, comparing a character box with a numeric one; reason: self-contradiction

// The dyadic OUTFIX over an ATOM, with u exactly `+/`. `2 +/\. (<1 2)` has
// shape `0 0` in jconsole and `0` here: an atom is one item, `1 + 1 - 2` is
// no outfix at all, and the answer is the empty frame with the cell shape
// the fold would have made. The same argument under `<\.` or `,/\.` agrees,
// as does every numeric type under `+/\.`, so this is the reference's own
// special code for `+/\.` again — the family the boxed empty frame above
// belongs to.
$ 2 +/\\. (<1 2)
? the same argument under `<\.` and `,/\.` agrees; reason: self-contradiction
$ 2 +/\\. (<<1 2)
? the same, one box deeper; reason: self-contradiction
$ 2 +/\\. (,<1 2)
? the same, written as a one-item list; reason: self-contradiction
$ 2 +/\\. ('a')
? the same over a character, so the family is not about boxes; reason: self-contradiction
$ 2 +/\\. (0 $ <0)
? the same where the argument has no item at all; reason: self-contradiction
$ 2 +/\\. (0 $ a:)
? the same with the empty box for the item; reason: self-contradiction
$ 8 +/\\. (0 $ a:)
? the same, with a wider left argument; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num|we-refuse:err:length_error:_reshape_of_an_empty_array verb=+ with=\. also=s:,u:,x:,!.,f.,::,M.,^:,&.,&.:,p:,q:,e.,-.,+.,*.,=,~:,<:,>:,<.,>.,-,*,%,^,%:,^.,|,j.,r.,o.,+:,-:,*:
$ 2 +/\\. (0 $ s: 'a')
? the same over symbols; reason: self-contradiction

// A CUT whose piece is EMPTY, folded by an EXTREMUM. `<./;._2 (,5)` is a
// domain error in jconsole; libjay answers the identity `_` the same fold
// gives outside a cut, `<./ (0 $ 0)` being `_` there too. The reference
// contradicts itself three ways over: `([: <./ ])`, `(<./ @ ,)` and
// `(<./ "1)` — the SAME function spelled around the insert — all answer
// `_` where the bare `<./` refuses; `+/`, `*/`, `+./`, `*./`, `=/`, `,/`,
// `-/`, `%/` and `<` all answer; and a FLOAT, character, boxed, rational
// or boolean piece answers where an integer or a complex one refuses. The
// EXTENDED piece answers 1 for the minimum and _1 for the maximum, which
// is no identity of either — the reference's own special code for `u/;.n`,
// not a rule.
<./ ;._2 (,5)
? the same fold spelled `[: <./ ]` answers `_` there; reason: self-contradiction
>./ ;._2 (,5)
? the same, the maximum's identity; reason: self-contradiction
<./ ;._1 (,5)
? the same under the other end's cut; reason: self-contradiction
<./ ;._2 (5 3 5)
? the same where only ONE of the two pieces is empty; reason: self-contradiction
<./ ;._2 (,1j2)
? the same over a complex piece, where a float one answers; reason: self-contradiction
<./ ;._2 (,1x)
? an extended piece answers 1, which is no minimum of anything; reason: self-contradiction
>./ ;._2 (,1x)
? the same, answering _1 for the maximum; reason: self-contradiction

// The PREFIX SCAN of a DIVISION insert against the insert itself. The last
// item of `%/\ (1 0 0)` is `%/ (1 0 0)` by the scan's own definition, and
// jconsole answers 0 for the one and `_` for the other: it reads `%/` as
// the alternating product `(*/ odd) % (*/ even)`, which is `0 % 0` here,
// where the fold `1 % (0 % 0)` is `1 % 0`. libjay scans by folding each
// prefix, so its answer is the reference's own `%/`.
%/\\ (1 0 0)
? the reference's own `%/` on the same prefix answers `_`; reason: self-contradiction
%/\\ (1 0 0 0)
? the same, one item longer; reason: self-contradiction

// The ROOTS of a polynomial of degree 3 or more with an INFINITE
// coefficient. jconsole refuses `p. (_ _ _ _)`; libjay answers the NaN
// roots the arithmetic gives. The refusal follows no rule the reference
// keeps: degree 1 and 2 answer with exactly those NaN roots
// (`p. (_ _)` and `p. (_ _ _)`), and so does `p. (0 0 1 1 _)` at degree 4
// with the same infinity — it is the reference's own root finder giving
// up on the numbers in front of it, not a statement about the domain.
p. (_ _ _ _)
? degree 2 with the same infinities answers the same NaN roots there; reason: self-contradiction
p. (1 1 1 _)
? one infinity is enough, wherever it stands; reason: self-contradiction
p. (_ _ _ _ _)
? the same at degree 4, where `p. (0 0 1 1 _)` answers; reason: self-contradiction

// DECODE where the LEFT argument's frame has no cell. `(0 2 $ 'a') #.
// (2 0 $ 0)` is `0 0` in jconsole, shaped by the RIGHT frame alone, where
// `(0 2 $ 0) #. (2 0 $ 0)` — the same two shapes with a NUMERIC left — is
// a length error: a character left cannot be more permissive about shape
// than a numeric one. `$ (2 0 3 $ 'a') #. (0 $ 0)` is `2` there, which no
// frame of a rank-1 verb gives. libjay agrees the frames the ordinary way
// and answers, or refuses, as those frames say.
(0 2 $ 'a') #. (0 $ 0)
? the reference drops the left frame and answers the atom 0; reason: self-contradiction
(0 2 $ 'a') #. (2 0 $ 0)
? the same two shapes with a numeric left are a length error there; reason: self-contradiction
(0 2 $ <0) #. (i. 3 0)
? the same, with a boxed left; reason: self-contradiction
$ (2 0 3 $ 'a') #. (0 $ 0)
? the reference's shape here is `2`, which no rank rule gives; reason: self-contradiction

// THE BINOMIAL AT NEGATIVE INFINITY UNDER A FRACTIONAL LEFT ARGUMENT.
// `x ! y` off the whole numbers is a ratio of gammas whose magnitude grows
// as |y|^x, so the limit at `__` is unbounded for a positive x and zero for
// a negative one, and `_1` is neither. The reference answers `_1` for
// `0.5 !` and `_0.5 !` and `__` for `1.5 !` and `2.5 !`, where at a finite
// negative y its own answers alternate in sign with both arguments
// (`1.5 ! _5` is `_`, `2.5 ! _5` is `__`, `0.5 ! _6` is `__`), and its
// `x ! _` for the same fractional x is a NaN error. libjay refuses both
// infinities for a fractional x, which is what the reference does at one of
// them.
0.5 ! __
? the limit is unbounded and the reference answers _1; reason: proven-wrong
_0.5 ! __
? the limit is zero and the reference answers _1; reason: proven-wrong
1.5 ! __
? the reference's limit does not alternate where its finite answers do; reason: self-contradiction

// A NEGATIVE LEADING RADIX in `x #: y` where the left argument has exactly
// TWO radices and both sides are integers. Everywhere else — one radix,
// three or more, a non-leading position, a float side, an extended side —
// the reference takes the residue the negative radix asks for, which is
// libjay's answer throughout: `_2 2 2 #: 11` is `0 1 1` there and here, and
// so are `_2 2 #: 5.0`, `_2 2 #: 11x` and `_2x 2 #: 11`. In the two-radix
// integer case alone it reads the negative radix as a ZERO one and hands
// back everything left over, so its own `_2 2 #: 11` is `5 1` where its
// `_2 2 #: 11x` is `_1 1`: the same question of the same numbers, answered
// two ways by the type they were written in.
_2 2 #: 5
? the reference's two-radix integer path reads a negative leading radix as a zero one; reason: self-contradiction
_1 2 #: 7
? the reference's two-radix integer path reads a negative leading radix as a zero one; reason: self-contradiction
_2 3 #: 11
? the reference's two-radix integer path reads a negative leading radix as a zero one; reason: self-contradiction
_10 10 #: 1234
? the reference's two-radix integer path reads a negative leading radix as a zero one; reason: self-contradiction

// THE POWER OF A NaN goes three ways, and round 8 measured them:
// `(_.) ^ 0.5` is the complex `_.j_.` there (the one-item widening the root
// shows), `_. ^ _.` and `(_. 1 2) ^ (_. 1 2)` are refused outright, and
// `(_. 1 2) ^ (_.)` is the float `_. _. _.`. libjay keeps the float NaN
// throughout, which is the reference's own answer for the longest of them.
// The DYADIC ROOT does the same: `2 %: (_.)` is refused there where
// `(_. 1 2) %: (0)` answers. The family rule under the first row carries
// all of it, and the root and the logarithm it also names are the DYADIC
// spellings: their monads were followed in round 9A.
(_.) ^ (0.5)
? the reference widens a one-item NaN power to the complex type, as it does the root; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num|differ:val:planes/num|they-refuse:val:atom/num|they-refuse:val:vector/num|they-refuse:val:table/num verb=%:,^.,^ also=%,-,+,*,<.,>.,<:,>:,+:,*:,|,o.,+.,*.,j.,!.,&.,&.:,",/,L:,S:,^:,:. answers=numeric
_. ^ _.
? the reference refuses the NaN power of a NaN where the same pair at three items answers; reason: self-contradiction
2 %: (_.)
? the reference refuses a one-item NaN root by a real where its own monadic root answers; reason: self-contradiction
0 ^ _.
? a zero base takes no NaN exponent there: the answer is a plain 0; reason: self-contradiction

// A LOGARITHM OF ZERO TO A LIST OF BASES. Every cell of it answers on its
// own there — `2 ^. 0` is `__`, `_0.5 ^. 0` is `_j_`, `_1 ^. 0` is `0j_`
// and `_2 ^. 0` is `__j_` — and a list of bases against a list of
// arguments answers too (`_0.5 ^. (0 1)` is `_j_ 0`), but a list of bases
// against the single 0 is a NaN error, `(_0.5 _0.5) ^. 0` included, whose
// two cells are the value the reference itself gives one at a time. libjay
// answers the cells.
(1.5 2.5 _0.5) ^. (0)
? every cell of this answers on its own there and the list of them does not; reason: self-contradiction
~ cause=they-refuse:val:atom/num|they-refuse:val:vector/num|they-refuse:val:table/num verb=^. also=%,*,+,-,+:,-:,*:,=,~:,<:,>:,|,j.,&.,&.:
(_0.5 _0.5) ^. 0
? every cell of this answers on its own there and the list of them does not; reason: self-contradiction
(1.5 _0.5) ^. 0
? every cell of this answers on its own there and the list of them does not; reason: self-contradiction
// `x:` OF A FLOAT THAT HAS NO SHORT EXACT RATIONAL. Both sides answer a
// rational within the default comparison tolerance and the Dictionary names
// neither: libjay answers the simplest one inside that tolerance — the
// continued fraction's first convergent to reach it — and the reference
// answers another, which is not a convergent of the double at all and which
// no rule black-box probing could reconstruct. The exact cases (`x: 0.1`,
// `x: 1r3 + 0.0`, `x: 123.456`) agree on both sides.
x: 2 ^ 0.5
? libjay answers the simplest rational within the comparison tolerance; reason: undefined
2 x: 2 ^ 0.5
? the same conversion, written as a numerator and a denominator; reason: undefined
x: 1e_15
? one convergent earlier than the reference's, and equally inside the tolerance; reason: undefined
x: 3.14159265358979
? the same, on a value with no short exact rational either; reason: undefined
// A sweep draws this family fresh every time and never the same spelling
// twice — `x: &.%: 2`, `x:&:o. 2`, `x: &.:j. (1 -/\. 2)` — because any verb
// that makes an irrational float feeds it. The rule below is what matches
// those: the conversion itself, a sentence that names only arithmetic which
// can BUILD such a float, and two answers that are both exact ratios.
// No `answers=` clause: both sides answer a RATIONAL, whose `r` is not a
// character any numeric answer class reads, so every one of them would be
// false here. The cause, the verb and the restricted `also=` carry the rule.
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num|differ:val:planes/num|differ:val:atom/text|differ:val:vector/text|differ:val:table/text verb=x: also=o.,%,%:,^.,^,*,+,-,+:,-:,*:,<.,>.,|,j.,r.,!.,&.,&.:,^:,=.,=:,:,`:,p.,#.,#:,F..,F.:,F:.,F::,L:,S:,;.,/.,M.,-.,@.,u:,b.,":,;:,I.,e.,A.

// INTERVAL INDEX OVER SYMBOLS. The reference's own `<` and `/:` order
// symbols by their NAME — `(s: <'a') < (s: <'b')` is 1 and
// `/: (s: <'b'), (s: <'a')` is `1 0` — but its `I.` does not use that
// order: it answers 1 where the value EQUALS the bound and 0 otherwise,
// which is the opposite of what its own `<` says about the same pair.
// libjay reads the bounds by the total array ordering, as it does for
// every other type.
(s: <'a') I. (s: <'b')
? the reference's own `<` puts these the other way round; reason: self-contradiction
(s: <'a') I. (s: <'a')
? the same pair, equal; reason: self-contradiction

// The dyadic OUTFIX over an argument holding a NaN, with u exactly `+/`.
// The reference totals the whole argument once and takes each outfix sum
// off that total, so a NaN in ANY item poisons every answer: its
// `2 +/\. (_. 1 2)` is `_. _.` where the pieces themselves are `2` and
// `_.`, and its `_2 +/\. (_.)` is `_.` where the piece is empty and its
// sum 0. `<./\.`, `,/\.` and `*/\.` over the same argument answer the
// pieces, `+/\` (the prefixes) answers them, and so do
// `2 ([: +/ ])\.` and `2 (+/@:])\.` — the SAME function spelled around
// `+/`. This is the reference's special code for `+/\.` disagreeing with
// its own ordinary path, the third spelling of the family pinned above.
2 +/\\. (_. 1 2)
? the same function spelled `[: +/ ]` answers the pieces there; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num|differ:val:planes/num verb=+,/,\. also=_.,-,*,%,^,%:,^.,+:,-:,*:,<:,>:,|,j.,&.,&.:,$,<,> answers=numeric
_2 +/\\. (_.)
? the same where the piece is empty, so the sum should be the fold's zero; reason: self-contradiction

// A FOUR-BYTE CODE THAT NAMES NO CHARACTER. `9 u:` and `10 u:` answer J's
// four-byte character type, whose element is thirty-two bits of anything:
// `3 u: 10 u: _2` is 4294967294 there, a number no codepoint names, and
// `3 u: 10 u: 4294967296` is 0, so the code is simply the residue those
// thirty-two bits leave. libjay's one character type holds a CODEPOINT, so
// a code outside the range Unicode names — and every surrogate — has no
// character here to answer with. Reading such a code is the same standing
// difference as the two-byte type itself: libjay has one character type
// where the reference has three.
10 u: _2
? a four-byte code is a value there and a codepoint here; reason: model
9 u: _3
? the same over the other form that answers the four-byte type; reason: model
~ cause=we-refuse:err:domain_error:_#_is_not_a_Unicode_codepoint|we-refuse:err:domain_error:_-#_is_not_a_Unicode_codepoint verb=u: also=x:,b.,|,/:,F.:,F..,F::,-,+,*,%,^,<.,>.,#.,#:,i:,i.,~.,^:,&,@,:,f.,::,M.,S:,L:,p:,q:,!.,j.,r.,o.,+:,-:,*:,>:,<:,=,~:,|.,{.,}.,/,\,\.,+.,*.,;.,{::,{,e.,~.,<,>,=:,=.,;:,s:,":,@.,`:,%.,A.,I.,-.,/.

// THE CRC OF A WIDE CHARACTER. `128!:3` reads a byte string, and the
// reference refuses one whose type is not the one-byte one: `128!:3 (u: 2)`
// is a domain error there although every code in it is a byte, because
// `u:` widened the type. libjay has one character type and reads the codes,
// so it answers the CRC of those bytes.
128!:3 (u: 2)
? the reference refuses the widened type, not the bytes in it; reason: model
~ cause=they-refuse:val:atom/num|they-refuse:val:vector/num verb=u: with=!: also=^:,&.,f.,::,M.,:

// THE FOLD CONJUNCTION ANSWERS TWO SHAPES TO ONE SENTENCE. In a fresh
// interpreter `$ ((+: F.. ,:) (2 3 $ i. 6))` is `1 6`; run the fold itself
// once first and the same `$` is `2 3`, twice over, so the answer depends
// on what the process has already evaluated and not on the sentence. The
// WRITING follows the first of them — `6 8 10 0 2 4` on one line — while
// `] ` of the same value and `0 + ` of it both write the two rows the
// `2 3` shape names. libjay answers the rank-2 result on every path and
// writes the two rows. `(<. F:: ,:) (i. 3 4)` is the same fold one
// conjunction over: `$` of it is `2 2 2 4` and the writing is of rows
// eight wide. A clause cannot name `,:` — the comma separates the list —
// so the rule leans on the neutral set for it and keeps `also=` to the
// arithmetic that cannot make an infinity of its own.
(+: F.. ,:) (2 3 $ i. 6)
? the reference's own `$` of this is `1 6` in a fresh process and `2 3` in a used one; reason: self-contradiction
~ cause=differ:val:vector/num|differ:val:table/num|differ:val:planes/num|differ:val:atom/num verb=F..,F.:,F:.,F:: also=+:,-:,*:,+,-,*,<.,>.,=,~:,<:,>:,e.,x:,!.,*.,+.,%,%:,^,^.,|,o.,j.,r.,&.,&.:,L:,S: answers=numeric

// INTERVAL INDEX OVER THE EXACT TYPES, WHERE THE REFERENCE CONTRADICTS THE
// SAME NUMBER WRITTEN INEXACTLY. `2 I. 3` is 1 there and `2 I. (3x)` is 0 —
// one value, two spellings, two answers — and `2 I. (1 2 3x)` is `0 1 0`,
// which puts the 1 on the bound itself and not on the item above it. Its
// own `(1 2) I. (1 2 3x)` is `0 1 2` and reads the extended items
// correctly, so the fault is in the one-bound search and not in the type.
// libjay answers the intervals: `0 0 1`.
2 I. (1 2 3x)
? the reference's own `2 I. 3` is 1 where its `2 I. (3x)` is 0; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num verb=I. also=x:,o.,_.,+,-,*,%,^,%:,|,<.,>.,j.,r.,!.,&.,&.:

// TWO ARGUMENTS TO `!` A DOUBLE CANNOT TELL APART, at the far end of the
// range this time. `(1p1) ! (9223372036854775806)` is 0 in the reference
// where the binomial is about 5.3e58: the answer is a difference of three
// log-gammas of arguments near 4.4e19, and the cancellation takes every
// digit it had. `(1e_9) ! (_9223372036854775806)` is `_1` there for the
// same reason, where the limit as the upper argument falls to zero is 1.
// libjay takes the ratio directly and answers the mathematics.
(1p1) ! (9223372036854775806)
? the reference's log-gamma difference loses every digit at this size; reason: proven-wrong
(1e_9 1 1e9) ! (_9223372036854775806)
? the reference's log-gamma difference loses every digit at this size; reason: proven-wrong

// THE RADIX COUNT OF AN EMPTY, WHERE THE REFERENCE CONTRADICTS ITSELF BY
// TYPE. `(1 2) #. (1 0 1 $ 5)` is a length error there — two radices for a
// last axis of one — while `(1r2 1r3) #. (1 0 1 $ 5)` is the empty of
// shape `1 0`: the same two radices against the same shape, written in the
// rational type instead. libjay applies the one check to both.
(1r2 1r3) #. (1 0 1 $ 5)
? the reference checks the radix count for integers and not for rationals; reason: self-contradiction
~ cause=we-refuse:err:length_error:_#_radices_for_#_digits verb=#.,#: also=$,x:,%,r.,j.,+,-,*,^,%:,^.,|,<.,>.,L:,S:,&.,&.:

// A BOXED ROOT FORM WHOSE ROOT IS ONE COMPLEX NUMBER, where the reference
// contradicts itself by rank. `p. (<c)` reads the box as the roots of a
// polynomial and answers its coefficients: `p. (< 2)` is `_2 1` and
// `p. (< 2 3j4)` — two roots, one of them complex — is `6j8 _5j_4 1`. The
// same verb over a box holding ONE complex number is an INDEX error there,
// `p. (< 0j0)` and `p. (< 1j0)` included, so it is not the imaginary part
// that is refused but the pairing of one root with the complex type. `p..`
// answers the same way. libjay reads the box the same at every length.
p. &.< (1ad45)
? the reference's own `p. (< 2 3j4)` answers where this one is an index error; reason: self-contradiction
~ cause=they-refuse:val:atom/num|they-refuse:val:vector/num|they-refuse:val:table/num verb=p.,p.. also=j.,r.,o.,^,^.,%:,+,-,*,%,+.,*.,<.,>.,|,&.,&.:,^:,L:,S:,x:,!.,f.,::,M.,:
p. &.< (0j1)
? the same with the imaginary unit for the root; reason: self-contradiction
p.. &.< (1ad45)
? the same under the polynomial derivative; reason: self-contradiction

// THE NaN THE ARITHMETIC MADE, SEEN THROUGH A SCAN, where the reference
// contradicts itself. `-/ (2 2 $ _ 1 _ 2)` is a NaN error there — `_ - _`
// has no value — and `-/\. (2 2 $ _ 1 _ 2)`, whose FIRST ITEM is that very
// reduction, answers `_. _1` over `_ 2`. The scan's own loop does not
// consult the check where its cells hold more than one atom; at atom cells
// it does, and `-/\. (_ _)`, `-/\ (_ _)`, `+/\. (_ __)` and `+/\ (_ __)`
// are all NaN errors. The same two arguments cannot have two answers, so
// libjay refuses on every path.
-/\\. (2 2 $ _ 1 _ 2)
? the reference's own `-/` of this is a NaN error, which contradicts it; reason: self-contradiction
~ cause=we-refuse:err:NaN_error:_`__-__`_has_no_value|we-refuse:err:NaN_error:_`__+___`_has_no_value|we-refuse:err:NaN_error:_a_residue_of_an_infinity_has_no_value|we-refuse:err:NaN_error:_`___`_has_no_value verb=/,\,\. also=-,+,*,%,|,^,^.,%:,<.,>.,*.,+.,o.,j.,r.,!,!.,&.,&.:,",@,@:,:,L:,S:,x:,F..,F.:,F::,^:,;.,M.,f.,::,#:,#.

// THE SIGN OF A BINOMIAL OF TWO NEGATIVE WHOLE NUMBERS, where the
// reference's parity is a DOUBLE'S parity. `x ! y` for a negative pair is
// `(_1) ^ (y - x)` times a binomial of their reflections, so
// `_1e17 ! _1` is `(_1) ^ 99999999999999999` — an odd power, and `_1`.
// The reference answers 1: 99999999999999999 needs 57 bits and the double
// it is computed in rounds it to the even 1e17. Its own `_1e15 ! _1` is
// `_1` and `_10 ! _1` is `_1`, both parities it can still see, so the
// rule is the reference's and only its arithmetic parts them. libjay
// holds the pair as the integers they are written as — `3!:0 (_1e17)` is
// the integer type on both sides — and answers the parity.
_1e17 ! _1
? the reference rounds 1e17-1 to an even double and loses the sign; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num verb=! also=%,*,+,-,+:,-:,*:,<:,>:,j.,&.,&.:,^,^:,|,<.,>.,x:,!.,",/,L:,S:
_1e16 ! _1
? the same one exponent down, the first size a double cannot tell apart; reason: self-contradiction

// EULER'S TOTIENT OF A VECTOR HOLDING AN INFINITY — ORACLE
// SELF-CONTRADICTION. `5 p: _` is a domain error in the reference and so is
// `5 p: 1.5`, yet `5 p: (_ 1 2)` answers `1 0 1` in the RATIONAL type,
// whose middle value contradicts its own `5 p: 1`, which is 1. Two of the
// three answers cannot both be the totient, so libjay refuses the vector as
// the reference refuses the atom.
5 p: (_ 1 2)
? the reference's own `5 p: _` and `5 p: 1` contradict this vector; reason: self-contradiction
~ cause=we-refuse:err:domain_error:_the_totient_needs_a_whole_number_that_is_not_negative verb=p: also=%,*,+,-,^,|,<.,>.,x:,",/,\,\.,L:,S:,&.,&.:,@,@:,^:,!.

// THE GRADE OF A VALUE CARRYING A NaN, WHEREVER THE COMPARISON IS
// CELL BY CELL — ORACLE SELF-CONTRADICTION. The reference's FLAT float
// grade is coherent and libjay follows it exactly: `/: (_. 1 2)` is
// `1 2 0` and `\: (_. 1 2)` is `0 2 1`, a NaN above every number and an
// infinity, stable among NaNs. The moment the items being ordered are
// ROWS of a table, BOXES or COMPLEX numbers the comparison is made cell
// by cell, and there the reference answers three things that cannot all
// be an order:
//
// - the SAME verb puts the same three values at opposite ends when they
//   are written in a different order: `/: (_.j1 1 2)` is `1 2 0`, the NaN
//   last, and `/: (1 2 _.j1)` is `2 0 1`, the NaN first;
// - the ascending and the descending grade of two IDENTICAL rows are the
//   same permutation and it is not the identity: `/: (2 2 $ _. 1 _. 1)`
//   and `\: (2 2 $ _. 1 _. 1)` are both `1 0`, where a stable grade of
//   equal items leaves them where they were in both directions;
// - `/: (2 ; _. ; 1)` is `0 1 2` and `\: (2 ; _. ; 1)` is `1 0 2`, and no
//   order of three values gives both.
//
// A three-row table reverses outright — `/: (3 3 $ _. 1 2)` is `2 1 0`
// where its three rows are identical — which is an insertion order and
// not a comparison. libjay orders a NaN above every number in every one
// of them, which is the reference's own flat answer.
/: (2 3 $ _. 1 2)
? the reference's own ascending and descending grades of these rows agree, which contradicts it; reason: self-contradiction
~ cause=differ:val:vector/num|differ:val:table/num|differ:val:table/text|differ:val:atom/num|differ:val:planes/num verb=/:,\\: also=$,;,<,>,j.,r.,o.,+,-,*,%,^,%:,^.,|,<.,>.,+.,*.,x:,!.,&.,&.:,",@,@:,^:,L:,S:,f.,::,M.,/,\\,\\.,#,{,},:
\\: (2 3 $ _. 1 2)
? the same argument graded downwards, which answers the same permutation; reason: self-contradiction
/: (3 3 $ _. 1 2)
? three identical rows reversed outright; reason: self-contradiction
\\: (_.j1 1 2)
? the complex grade, whose ascending answer puts the NaN at the other end; reason: self-contradiction
/: (1 2 _.j1)
? the same three complex values written in another order, and the NaN moves ends; reason: self-contradiction
\\: (_. ; 1)
? the boxed grade, where the reference's own ascending answer is the identity; reason: self-contradiction
\\: (2 ; _. ; 1)
? three boxes whose two grades are not each other's reverse; reason: self-contradiction
(_. ; 1) \\: (_. ; 1)
? the grade-select built on the same comparison; reason: self-contradiction

// THE INTERVAL INDEX OVER A COMPLEX BOUND CARRYING A NaN, which is the
// same cell-by-cell order one verb along: `(_.) I. (1j2)` is 1 there and
// `(1j2) I. (_.)` is 1 as well, so the value is above the bound and the
// bound above the value. libjay reads a NaN as above every number, as its
// grade does.
(_.) I. (1j2)
? the reference puts each of the pair above the other; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num verb=I. also=j.,r.,o.,+,-,*,%,^,%:,^.,|,<.,>.,+.,*.,x:,!.,&.,&.:,",@,@:,^:,L:,S:,f.,::,M.,$,<,>,;

// THE COMPLEX GCD OF A VALUE PAST THE DOUBLE'S WHOLE-NUMBER RANGE, where
// NEITHER engine answers a common divisor. `(_9223372036854775806) +. (3j4)`
// is 3 in the reference and `3j4` here, and `3` divides `3j4` no more than
// `3j4` divides the integer: 3+4i over 3 is not a Gaussian integer, and
// _9223372036854775806 is not a multiple of 5, which is what 3+4i would
// need to divide it. Both are float Euclids whose residue chain has no
// digits left to cut at 9e18, stopping at different steps. The true
// divisor is 1.
(_9223372036854775806) +. (3j4)
? neither answer divides both arguments; two float Euclids at 9e18; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num verb=+.,*. also=j.,r.,",@,@:,^:,&.,&.:,L:,S:,f.,::,M.,/,\\,\\.,x:,!.,$,<,>,;,o. answers=numeric

// THE POWER AND THE ROOT THAT REACH INTO THE COMPLEX PLANE, WHERE THE
// REFERENCE REFUSES AN ANSWER IT HAS ALREADY WRITTEN DOWN — ORACLE
// SELF-CONTRADICTION. `(_.) ^ (0.5)` is `_.j_.` there and
// `% ((_.) ^ (0.5))` is `_.j_.` as well, so the negated exponent —
// `x ^ _y` being `% (x ^ y)` — has an answer the reference itself gives;
// `(_.) ^ (_0.5)` is a limit error all the same. The ROOT parts the same
// way: `x %: y` is `y ^ % x` by the Dictionary, and `(_j_) ^ (% 2)` is
// `_.j_.` there where its own `2 %: (_j_)` is `_j_`. Neither pair can both
// be right, so there is no rule to follow, and libjay answers the value
// IEEE complex arithmetic gives at every shape. The whole family is one
// mechanism: the reference composes `^ y * ^. x` in doubles and reports a
// LIMIT ERROR wherever that composition loses its direction, which is not
// a statement about the domain but about its own floating point.
(0.5) ^ (0j_)
? the reference's own `% ((_.) ^ (0.5))` contradicts its refusal of `(_.) ^ (_0.5)`; reason: self-contradiction
~ cause=they-refuse:val:atom/num|they-refuse:val:vector/num|they-refuse:val:table/num|they-refuse:val:planes/num|differ:val:atom/num|differ:val:vector/num|differ:val:table/num|differ:val:planes/num verb=^,%: also=%,-,+,*,<.,>.,|,o.,+.,*.,j.,r.,x:,!.,&.,&.:,",/,L:,S:,$,<,>,;
(2) %: (_j_)
? the reference's own `(_j_) ^ (% 2)` is `_.j_.` where this is `_j_`; reason: self-contradiction

// THE CIRCLE FUNCTION THAT REFUSES ITS OWN COMPOSITION — ORACLE
// SELF-CONTRADICTION. `0 o. y` IS `%: 1 - y ^ 2` by the Dictionary, and the
// reference writes that composition out for the very argument it refuses:
// `%: (1 - ((_.) ^ 2))` is `_.j_.` there — `(_.) ^ 2` is `_.` and
// `1 - (_.)` is `_.` — while `0 o. (_.)` is a NaN error. `0 o. (0j_)` and
// the negative forms part the same way. libjay answers the composition.
(0) o. (_.)
? the reference's own `%: (1 - ((_.) ^ 2))` is `_.j_.` where this is refused; reason: self-contradiction
~ cause=they-refuse:val:atom/num|they-refuse:val:vector/num|they-refuse:val:table/num|they-refuse:val:planes/num|differ:val:atom/num verb=o. also=%:,^,-,+,*,%,|,<.,>.,j.,r.,x:,!.,&.,&.:,",/,L:,S:,$,<,>,;

// DIVIDING BY A ZERO WRITTEN AS A RATIONAL, WHERE THE REFERENCE ANSWERS THE
// OTHER INFINITY — ORACLE SELF-CONTRADICTION. `(0.5) % 0` is `_` there and
// `(0.5) % (0x)` is `_`, but `(0.5) % (0r1)` — the same zero in the
// rational type — is `__`, and `(__) % (0r1)` is `_` where `(__) % 0` is
// `__`. One zero, three spellings, and the rational one reverses the sign.
// libjay divides by the value.
(0.5) % (0r1)
? the reference's own `(0.5) % (0x)` is `_` where this is `__`; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num|differ:val:table/num|differ:val:planes/num verb=% also=x:,+,-,*,<.,>.,|,j.,r.,!.,&.,&.:,",/,L:,S:,$,<,>,;

// ---------------------------------------------------------------------
// THE MODIFIER GRID (docs/testing.md, "The modifier grid"). Where the
// reference answers one way under a modifier and another way as the bare
// dyad, and the two answers are its own.

// The min-insert and the max-insert fold from the operation's IDENTITY —
// `_` for the minimum, `__` for the maximum — and a NaN, which loses every
// comparison, never displaces it. The insert of two NaNs IS `_. <. _.`,
// which the reference's own `<.` answers with a NaN, and the SCAN of the
// same list — whose last item is that very insert — answers `_.` there
// too. Only the standalone insert takes the identity.
<./ ((_.) , (_.))
? `(_.) <. (_.)` is `_.` there and `<./\ ((_.) , (_.))` is `_. _.`, whose last item is this insert; reason: self-contradiction
>./ ((_.) , (_.))
? `(_.) >. (_.)` is `_.` there and `>./\ ((_.) , (_.))` is `_. _.`, whose last item is this insert; reason: self-contradiction
((<.) (1 : 'u/ y')) ((_.) , (_.))
? the same insert written inside an explicit adverb; reason: self-contradiction
((>.) (1 : 'u/ y')) ((_.) , (_.))
? the same, for the maximum; reason: self-contradiction

// The SUFFIX SCAN makes the NaN its own insert refuses. Every row below
// folds a pair the reference will not fold on its own: `-/ ((__) , (__))`
// and `%/ ((_) , (_))` are NaN errors there, and the suffix scan of a table
// whose rows hold those values answers `_.` for exactly that fold.
-/\\. (2 2 $ (__))
? `-/ ((__) , (__))` is refused there and this scan's first row is that insert; reason: self-contradiction
%/\\. (2 2 $ (_))
? `%/ ((_) , (_))` is refused there and this scan's first row is that insert; reason: self-contradiction
%/\\. (2 2 $ (__))
? the same quotient over the negative infinity; reason: self-contradiction
%/\\. (2 2 $ (_j0))
? the same over a complex infinity with no imaginary part; reason: self-contradiction
%/\\. (2 2 $ (0j_))
? the same over a complex infinity with no real part; reason: self-contradiction
%/\\. (2 2 $ (_j_))
? the same over the complex infinity in both parts; reason: self-contradiction

// And the other way round: the quotient of a complex NaN is refused under
// an INSERT and answered as a dyad. `(2) % (_.j_.)` and
// `(_.j_.) % (_.j_.)` are both `_.j_.` in the reference, which is what
// every one of these inserts folds.
%/ ((_.j_.) , (_.j_.))
? `(_.j_.) % (_.j_.)` is `_.j_.` there, which is this insert; reason: self-contradiction
%/\\ ((_.j_.) , (_.j_.))
? the same fold under a prefix scan; reason: self-contradiction
%/\\. ((_.j_.) , (_.j_.))
? the same fold under a suffix scan; reason: self-contradiction
%/\\ (2 2 $ (_.j_.))
? the same fold over the rows of a table; reason: self-contradiction

// A complex infinity divided by a LIST is refused there, though the same
// division answers atom against atom and list against list. The rank
// conjunction makes that cell, so `%"0 _` parts the two engines wherever
// the frame pairs the atom with the whole.
(_j_) % (2 , 2)
? `(_j_) % (2)` is `_j_` there and `((_j_) , 2) % (2 , 2)` is `_j_ 1`; reason: self-contradiction
((_j_) , (_j_)) %"0 _ ((2) , (2))
? the same division under a rank frame, which makes that atom-against-list cell; reason: self-contradiction
((_j_) , (_j_)) %/ ((2) , (2))
? the same division through an outer product, which makes the same cell; reason: self-contradiction

// THE SIGN OF A ZERO THROUGH AN UNDER. `% (__)` is a zero in both engines
// and the display does not say which one; `&.%` asks, because the obverse
// divides by it again. The reference keeps the sign through `2 * y` and
// loses it through `+: y`, which the Dictionary defines as that very
// product.
+:&.% (__)
? `% (2 * (% (__)))` is `__` there and `% (+: (% (__)))` is `_`, though `+: y` is `2 * y`; reason: self-contradiction
+:&.% ((__) , (__))
? the same doubling over a list; reason: self-contradiction
o.&.% (__)
? `% (o. (% (__)))` is `_` there where `% (2 * (% (__)))` is `__`: the same multiplication by a constant, and only one of them keeps the sign; reason: self-contradiction
o.&.% ((__) , (__))
? the same circle function over a list; reason: self-contradiction
|&.% (__)
? the reference's magnitude of a negative zero is NEGATIVE — `% (| (% (__)))` is `__` there — where a magnitude is never negative and its own `| (__)` is `_`; reason: proven-wrong
|&.% ((__) , (__))
? the same magnitude over a list; reason: proven-wrong

// A LOGARITHM WITH A NaN OR AN INFINITE BASE AND A CHARACTER OR A BOXED
// RIGHT ARGUMENT ANSWERS NOTHING THERE: no value, no error, an empty line.
// The interpreter survives it — the next sentence of the same session
// answers — so it is silence rather than a crash, and there is no answer
// to hold libjay to. Every other base refuses as libjay does
// (`(2) ^. ('a')` is a domain error in both).
(_.) ^. ('a')
? the reference prints no answer and no error at all; reason: abort
(_.) ^. (<2)
? the same silence over a boxed right argument; reason: abort
('a') ^.~ (_.)
? the same silence, reached through a commute; reason: abort
(<2) ^.~ (_.)
? the same silence over a box, reached through a commute; reason: abort
((_.) , (_.)) ^."0 _ (('a') , ('a'))
? the same silence under a rank frame; reason: abort
((_.) , (_.)) ^."0 _ ((<2) , (<2))
? the same silence over boxes under a rank frame; reason: abort
((_.) , (_.)) ^./ (('a') , ('a'))
? the same silence through an outer product; reason: abort
((_.) , (_.)) ^./ ((<2) , (<2))
? the same silence over boxes through an outer product; reason: abort

// A NaN COMPARED WITH AN EXACT OPERAND ANSWERS BY WHICH SIDE IS THE ARRAY.
// `(_.) = (0x)` is 1 there and `((0x) , 2) = (_.)` is `1 1` — the
// reference reads a NaN as matching every number, which is what libjay
// does everywhere — but put the NaN in the list and the same comparison is
// 0. Nothing about the values changed.
((_.) , 2) = (0x)
? `(_.) = (0x)` is 1 there and `((0x) , 2) = (_.)` is `1 1`; reason: self-contradiction
((_.) , 2) <: (0x)
? the same asymmetry through the non-strict comparison; reason: self-contradiction
((_.) , (_.)) ="_ 0 ((0x) , (0x))
? the same comparison under a rank frame; reason: self-contradiction
((_.) , (_.)) <:"_ 0 ((0x) , (0x))
? the same under a rank frame; reason: self-contradiction
((0x) , (0x)) ="0 _ ((_.) , (_.))
? the frame the other way round, which makes the same cell; reason: self-contradiction
((0x) , (0x)) <:"0 _ ((_.) , (_.))
? the same the other way round; reason: self-contradiction

// The same contradiction over the COMPLEX types, where libjay's fold and
// its scan now agree with one another rather than with the reference's two
// answers: the insert of these rows is refused there (and here), and the
// scan of the very same rows answers the NaN that insert would have made.
*/\\ (2 2 $ (_j_))
? `*/ (2 2 $ (_j_))` is refused there and this scan's last row is that insert; reason: self-contradiction
*/\\. (2 2 $ (_j_))
? the same insert under a suffix scan; reason: self-contradiction
-/\\. (2 2 $ (_j0))
? `-/ (2 2 $ (_j0))` is refused there and this scan's first row is that insert; reason: self-contradiction
-/\\. (2 2 $ (0j_))
? the same over a complex infinity with no real part; reason: self-contradiction
-/\\. (2 2 $ (_j_))
? the same over the complex infinity in both parts; reason: self-contradiction

// THE OUTER PRODUCT AGAINST THE DYAD IT IS MADE OF. `x u/ y` puts every
// item of x against every item of y under u, so every cell of it is a
// sentence the two engines already agree on: `(0.5) <. (_.)` is `_.` in
// both and `(_.) <. (0.5)` is 0.5 in both. Fold the same pair through an
// outer product and the reference answers the OTHER one, cell for cell,
// for the minimum, the maximum and the four comparisons. libjay's outer
// product answers what its own dyad answers.
((0.5) , (0.5)) <./ ((_.) , (_.))
? `(0.5) <. (_.)` is `_.` there and this outer product of the pair is 0.5; reason: self-contradiction
~ cause=differ:val:table/num|differ:val:vector/num|differ:val:atom/num verb=<.,>.,<:,>:,=,> with=/ also=,,_.,+,-,*,%,x:,0x
((_.) , (_.)) >./ ((0.5) , (0.5))
? `(_.) >. (0.5)` is 0.5 there and this outer product of the pair is `_.`; reason: self-contradiction
((_.) , (_.)) =/ ((0.5) , (0.5))
? `(_.) = (0.5)` is 0 there and this outer product of the pair is 1; reason: self-contradiction

// A FLOOR COMPOSED ON A LOGARITHM, over an exact zero. `^. (0x)` is `__`
// there and `<. (__)` is `__`, and `(<. @ ^.) (0)` — the same composition
// over the ordinary zero — is `__` too; only the EXACT zero through the
// composition answers the least integer a double holds, which is the
// reference's own fused form converting a value it has no integer for.
(<. @ ^.) (0x)
? `<. (^. (0x))` is `__` there and the composition of the two is `_9223372036854775808`; reason: self-contradiction
~ cause=differ:val:atom/num|differ:val:vector/num verb=<. with=^. also=@,@:,[:,x:,r.,0x,0r1
(<. @: ^.) (0x)
? the same at the composition that joins the two whole; reason: self-contradiction
(<. @ ^.) (0r1)
? the same over the rational zero; reason: self-contradiction

// A WEIGHED SUM WHOSE BASE IS A NaN AND WHOSE DIGITS ARE ZERO. `x #. y`
// weighs each digit by the running product of the bases after it, so the
// value here is `(_. * 0) + 0`, and the reference's own `(_.) * (0)` is 0.
// It answers `_.` all the same, where the same base against a digit of 1 —
// which really does carry the NaN — is `_.` in both.
((_.) , (_.)) #. ((0) , (0))
? `(_.) * (0)` is 0 there and this weighed sum of that product is `_.`; reason: self-contradiction
~ cause=differ:val:atom/num verb=#. also=/,,,x:,*,+,_.,0x
((_.) , (_.)) #. ((0x) , (0x))
? the same over the exact zero; reason: self-contradiction

// A LEAST-SQUARES SYSTEM WHOSE NORMAL EQUATIONS OVERFLOW. Scaling a system
// by a constant does not change its solution, and the reference answers 1
// for `((1e150) , (1e150)) %. ((1e150) , (1e150))`; the same system at
// 1e160, where the sum of the squared coefficients passes what a double
// holds, is refused there. The solution exists and libjay finds it.
((1e300) , (1e300)) %. ((1e300) , (1e300))
? the same system at 1e150 is 1 there, and scaling one does not change its solution; reason: proven-wrong
~ cause=they-refuse:val:atom/num|they-refuse:val:vector/num|they-refuse:val:table/num verb=%. also=F..,F.:,F:.,F::,+,-,*,%,<.,>.,+:,-.,,,,.,],[,p.,$,1e300
((1e160) , (1e160)) %. ((1e160) , (1e160))
? the smallest scale measured at which the reference's own method overflows; reason: proven-wrong

// THE SYMBOL TABLE'S OWN FORMS, WHICH ARE THE INTERPRETER'S DATA STRUCTURE
// AND NOT A VALUE THE LANGUAGE DEFINES. `0 s: y` and `1 s: y` DUMP a
// symbol table: `$ 0 s: 2` is `2000 11` there, a table of slot counters
// with one machine address in it (`3080238136`) and one
// `_9223372036854775808`, and `6 s:`, `7 s:`, `_1 s:` and `_2 s:` number a
// symbol within it — how many slots it holds, which are in use, how it
// hashes them. Matching any of that would mean reproducing an
// implementation's hash table rather than J, so libjay answers "not
// implemented yet" and docs/coverage.md names them as gaps. The NAME forms
// `2 s:` to `5 s:` are implemented and agree.
0 s: (2)
? the reference answers with its own symbol table, which is no value the language defines; reason: undefined
~ cause=gap:err:not_supported_yet:_the_symbol-table_form_(#_s:)_is_not_supported_yet verb=s: also=x:,u:,b.,|,/:,F.:,F..,F::,-,+,*,%,^,<.,>.,#.,#:,i:,i.,^:,&,@,:,f.,::,M.,p:,q:,!.,j.,r.,o.,+:,-:,*:,>:,<:,=,~:,|.,{.,}.,+.,*.,;.,{::,e.,=:,=.,;:,":,@.,`:,%.,A.,I.,-.,/.,p..,E.,L.,$.,".,3!:,!,a:,%:,^.
0 s: (0)
? the same, where the table it dumps holds one row for the argument; reason: undefined
1 s: (0 $ 0)
? the second dumping form, over an argument with no items; reason: undefined

